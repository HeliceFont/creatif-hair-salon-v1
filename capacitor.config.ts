import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.creatifhairsalon.app',
  appName: 'CreatifHairSalon',
  webDir: 'dist'
};

export default config;
